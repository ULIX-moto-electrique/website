{
  "name": "ulix-landing-page",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev --host",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "predeploy": "npm run build && node ./scripts/create-nojekyll.js",
    "deploy": "gh-pages -d dist --nojekyll --dotfiles"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "astro": "^5.11.0",
    "swiper": "^12.0.3",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
